# Commands

- `/nick <name>` - get a name, otherwise user stays anonymous
- `/join <name>` - join a room, if doesn't exists new room is created. User
can be only in one room at a time.
- `/rooms` - list all the rooms
- `/msg <msg>` - broadcast msg to everyone in the room
- `/quit` - disconnects from chat server.